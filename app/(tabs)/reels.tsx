import { View } from "react-native";
import ReelPage from "../reels_content";


export default function Reels() {
    return (
        <View style={{ flex: 1, backgroundColor: "#ffff" }}>
            <ReelPage />
        </View>
    );
}